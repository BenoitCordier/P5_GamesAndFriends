{% extends 'base.html.twig' %}

{% block title %}Games & Friends - Modifier mes jeux{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4">Modifier mes jeux</h1>
    <a href="{{ path('home.index') }}" class="btn btn-outline-primary">
        Revenir à l'accueil
    </a>

    {{ form_start(form) }}

    <div class="form-group">
        {{ form_label(form.games) }}
        {{ form_widget(form.games) }}
        <div class="form-error">
            {{ form_errors(form.games) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_row(form.submit) }}
    </div>

    {{ form_end(form) }}
</div>

</div>
{% endblock %}